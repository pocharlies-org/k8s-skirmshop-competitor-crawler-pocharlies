# k8s-skirmshop-competitor-crawler-pocharlies

Disabled k8s skeleton for `sauvage:/home/ubuntu/skirmshop/skirmshop-competitor-crawler`.

The compose project defines a long-running crawler that pushes into
Skirmshop Brain and uses Firecrawl. This skeleton keeps it at `replicas: 0`
until rate limits, source image, and target brain instance are confirmed.
