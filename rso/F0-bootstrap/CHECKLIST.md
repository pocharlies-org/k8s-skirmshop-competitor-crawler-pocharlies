# RHO Checklist — F0 Bootstrap (lista + fingerprint + índices)

> Formato RHO probado. Fase **F0** del `RSO-MASTER-PLAN.md`. Criterios derivados del plan aprobado §6 (F0) y §8.
> Marca `[x]` SOLO con `Evidence:` directa (comando/archivo/log/Cypher). No debilites ni borres criterios.
> La auditoría de Codex/RSO valida re-ejecutando los comandos de evidencia, no leyendo el texto.

## Objective
- [ ] Dejar F0 lista para auditoría: fuente del crawler en clon local, lista top10 ES/top20 EU validada en `CompetitorSource`, `fingerprint.json` al 100% de dominios, índices del grafo definidos/creados al arrancar el brain, y **cero** requests de escritura/cart a competidores.

## Directives
- [ ] Claude = EJECUTOR bajo la Engineering Constitution (`/home/dibanez/k8s/CLAUDE.md`). Codex = PMO/auditor. No avanzar de fase sin PASS.
- [ ] F0 es **read-only salvo bootstrap** de repo/registry: solo escrituras permitidas = traer la fuente al clon local y poblar/curar `CompetitorSource`. Hacia los competidores, **solo lectura**.
- [ ] **Cero** requests de cart-probe o de escritura contra dominios de competidores en F0 (eso es F4).
- [ ] Fingerprint con Firecrawl en **lectura**, respetando `robots.txt`/`Crawl-delay`, UA honesto, sin eludir challenges ni resolver CAPTCHAs.
- [ ] No re-hardcodear dominios: la fuente única de la lista es Prisma `CompetitorSource`.
- [ ] Git: rama `codex/competitor-crawler-F0-bootstrap`; `fetch`+rebase antes de commit; push inmediato; nunca force-push ni tocar `deploy/prod`.
- [ ] Cero hacks/workarounds; si algo se bloquea, documentar `[blocked]` con motivo y escalar a Codex (p.ej. dominio detrás de CAPTCHA interactivo).

## Acceptance Criteria
- [x] **Fuente del crawler en clon local.** La fuente de `sauvage:/home/ubuntu/skirmshop/skirmshop-competitor-crawler` está traída al clon local del repo (no solo en sauvage). Evidence: `rsync -av --exclude=.git/.pytest_cache/__pycache__/.mypy_cache/.venv/node_modules/*.egg-info/README.md sauvage:.../skirmshop-competitor-crawler/ ./` → `git status` muestra `?? .gitignore Dockerfile config.yaml docker-compose.yml requirements.txt src/ tests/`; árbol importado = 15 ficheros (`src/` 8, `tests/` 2) con **md5 idéntico** local vs sauvage; detalle y checks en `rso/F0-bootstrap/backend.report.md`. **Nota:** estado en working tree (sin commit/push, por directiva); pendiente de commit por PMO/Codex.
- [blocked: ranking provisional] **Lista top10 ES / top20 EU derivada por tráfico real + curación.** Derivada con MCP SimilarWeb y validada por el usuario (no inventada). Evidence: user delegated autopilot; `data/competitors/target-domains.autopilot.json` contains 10 Spain + 20 Europe PMO-curated domains with public Similarweb URLs and legacy seed provenance. SimilarWeb MCP remained unavailable, so `rso/F0-bootstrap/target-list.report.md` marks ranking provisional and business validation pending.
- [blocked: migration prepared, DB not applied] **Lista poblada en `CompetitorSource` (fuente única).** Las 30 fuentes (10 ES + 20 EU) curadas están en Prisma `CompetitorSource`, no re-hardcodeadas. Evidence: prepared migration `skirmshopshopifyapp` commit `e275329` (`prisma/migrations/20260624140000_competitor_source_airsoft_autopilot_targets/migration.sql`) with 30 idempotent upserts. Not applied to DB in this cycle.
- [x] **`fingerprint.json` cubre el 100% de los dominios.** Un registro por cada dominio de la lista, sin omisiones. Evidence: `data/competitors/fingerprint.json` count=30 and 30 unique domains; PMO validation command printed `fingerprints 30 30 30`.
- [x] **Cada fingerprint trae todos los campos.** `platform`, `tier(green|yellow|red)`, `has_structured_data`, `has_visible_stock`, `robots_crawl_delay`, `antibot` presentes en cada entrada. Evidence: `scripts/fingerprint_domains.py` validates required fields; PMO validation printed `missing_fields []`.
- [x] **`silverback-airsoft.com = red`.** Clasificado `tier=red` por Cloudflare + CAPTCHA interactivo. Evidence: `data/competitors/fingerprint.json` has `silverback-airsoft.com` as `tier=red`, `antibot=captcha`, `http_status=200`, platform `woocommerce`.
- [x] **Recuento por tier.** Conteo de dominios por `tier` (green/yellow/red) reportado. Evidence: `rso/F0-bootstrap/fingerprint.report.md`; PMO validation counted `{'red': 16, 'yellow': 9, 'green': 5}`.
- [blocked: live Cypher/rollout no verificado] **Índices del grafo definidos/creados al arrancar el brain.** `Product.id`, `Product.sku`, `CompetitorProduct.*` definidos y creados al startup del brain. Evidence: `skirmshop-brain-v2` commit `f53b552` en rama `codex/product-recommendations-20260616` añade `GRAPH_INDEXES`, `CompetitorProduct(id,domain,url,source_id,sku,brand)`, `CompetitorStore(id,domain)`, hook startup best-effort en `src/app.py`, delegación de `scripts/business_sources.py`, y tests `tests/unit/test_graph_indexes.py`; PMO/verifier ejecutaron `py_compile` + `pytest tests/unit/test_graph_indexes.py tests/unit/test_business_sources.py tests/unit/test_config.py -q` → 8 passed. Sigue bloqueado para PASS global hasta verificar runtime/Cypher o rollout real.
- [x] **Cero requests de escritura/cart a competidores en F0.** Ninguna petición de cart-probe ni de escritura hacia dominios de competidores durante F0. Evidence: `rso/F0-bootstrap/security.report.md` verifica `replicas:0`, F0 fue import+tests, no live crawl; no primitives cart/add/checkout/login/write contra competidores.

## Specialist Checks
- [x] **rho-researcher** — inventario de la fuente del crawler en sauvage y su importación al clon local. Evidence: `rso/F0-bootstrap/researcher.report.md`.
- [x] **rho-architect** — esquema de `fingerprint.json` (campos/tipos) + definición de índices del grafo al arranque del brain. Evidence: `rso/F0-bootstrap/architect.report.md`; reporta bloqueos, no PASS global.
- [x] **rho-security** — postura anti-bot/robots: tiers, `silverback=red`, `Crawl-delay` honrado, sin eludir challenges, cero cart/escritura en F0. Evidence: `rso/F0-bootstrap/security.report.md`; cero cart/escritura verificado, robots/fingerprint bloqueados.
- [blocked: F0 no puede cerrar PASS] **Codex (auditor/verifier)** — PASS/FAIL independiente re-ejecutando los comandos de evidencia (no leyendo el texto). Evidence: PMO re-ejecutó `py_compile`, `pytest` en venv, md5 local vs sauvage, `git diff --check`, inspección de reports; `rho-verifier` completó `rso/F0-bootstrap/verifier.report.md` y confirmó bootstrap/housekeeping PASS pero F0 global bloqueado por SimilarWeb/fingerprint/índices.

## Status (log datado, append-only)
- 2026-06-24T02:06:11+02:00 — OPEN: artefactos F0 bootstrap preparados. Claude CLI produjo `RSO-MASTER-PLAN.md` y `rso/F0-bootstrap/CHECKLIST.md` tras dos intentos inicialmente mudos, pero no devolvió report ni creó `HANDOFF.md`; Codex/RSO completó `rso/F0-bootstrap/HANDOFF.md` como excepción limitada a documentación de orquestación, no código de producto. Todos los criterios F0 siguen en `[ ]`, pendientes de ejecución por Claude y auditoría Codex.
- 2026-06-24T02:47:00+02:00 — PARTIAL/BLOCKED: Claude CLI importó la fuente real desde `sauvage` y produjo `researcher`, `backend`, `security` y `architect` reports. Codex/RSO re-ejecutó evidencia: md5 local vs sauvage sin diff, `py_compile` OK, `pytest` en venv 11 passed, `git diff --check` limpio. F0 NO cierra PASS: SimilarWeb MCP no está expuesto, no hay lista top10/top20 validada, no se generó `fingerprint.json`, `silverback=red` no está verificado, y faltan índices `CompetitorProduct`/`CompetitorStore` en Brain con `skirmshop-brain-v2` actualmente en rama paralela `codex/product-recommendations-20260616`. Excepción PMO: Codex actualizó este checklist para reconciliar evidence/status; no tocó código de producto.
- 2026-06-24T13:20:00+02:00 — VERIFIER DONE: `rho-verifier` independiente completó `rso/F0-bootstrap/verifier.report.md`. Veredicto: source import/test/repo-clean/remote-head/no-F1 PASS; F0 global sigue PARTIAL/BLOCKED por SimilarWeb, `fingerprint.json`, `silverback=red` e índices Brain.
- 2026-06-24T13:35:00+02:00 — BRAIN INDEX CODE DONE: Claude CLI implementó índices Brain en `skirmshop-brain-v2` y Codex/RSO auditó/pusheó commit `f53b552` en rama `codex/product-recommendations-20260616`: `GRAPH_INDEXES` común, hook startup best-effort, `CompetitorProduct`/`CompetitorStore`, y tests 8 passed. F0 global sigue bloqueado por SimilarWeb/fingerprint/silverback y por falta de verificación runtime/Cypher de índices creados en un Brain desplegado.
- 2026-06-24T14:05:00+02:00 — TARGET/FINGERPRINT DONE / F0 STILL BLOCKED: Claude CLI creó lista autopilot `data/competitors/target-domains.autopilot.json` (10 ES + 20 Europe), fingerprint read-only `data/competitors/fingerprint.json` (30/30, tier counts red=16/yellow=9/green=5, `silverback-airsoft.com=red/captcha`), y Codex/RSO auditó `py_compile`, campos requeridos, conteos y cero POST/cart. `skirmshopshopifyapp` commit `e275329` prepara `CompetitorSource` con 30 upserts idempotentes, sin aplicar migración a DB. F0 sigue bloqueado por ranking provisional, DB `CompetitorSource` no aplicada e índices Brain sin Cypher/runtime verificado.
