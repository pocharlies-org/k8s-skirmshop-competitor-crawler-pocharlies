# RHO Checklist - F4 Cart-Probe

> Fase **F4** del `RSO-MASTER-PLAN.md`.
> Gate previo requerido: F0/F1/F2/F3/F5 PASS. Orden aprobado: `F0 -> F1 -> (F2 || F5) -> F3 -> F4 -> F6 -> F7`.
> Marca `[x]` SOLO con `Evidence:` directa (comando/archivo/log/SQL output). No abrir F6/F7 sin PASS F4.

## Objective
- [blocked] Implementar y validar `skirmshop-stock-prober`: cart-probe agresivo pero acotado, con busqueda binaria de cantidad maxima, limpieza de carrito verificada, kill-switch 403/429/challenge, calibracion vs stock visible en muestra de 10, e integracion F3 append-only (`stock_qty`, `stock_method=cart_probe`) sin tocar F6/F7. Evidence: mock/dry-run scope PASS in `codex-audit.report.md`; full objective blocked because sample-10 live calibration has no approved green Shopify/Woo target (`researcher.report.md`, `verifier.report.md`, `f4b-target-discovery.report.md`).

## Directives
- [x] Claude CLI = EJECUTOR; Codex = RSO/PMO/auditor. Claude implementa/prueba; Codex re-ejecuta evidencia antes de cerrar F4. Evidence: Claude reports `researcher.report.md`, `architect.report.md`, `backend-*.report.md`, `devops.report.md`, `security.report.md`, `verifier.report.md`; Codex audit `codex-audit.report.md`.
- [x] F4 se abre solo tras F3 PASS. Evidence: crawler commit `e23659b`; infra commit `a6e64e9`; F3 checklist Objective `[x]`.
- [x] Prohibido checkout, login, cuentas, aceptacion de ToS, CAPTCHA solvers, bypass anti-bot o acciones que degraden sitios. Evidence: `security.report.md`; `verifier.report.md`; `rg` scan of `src/prober` found no forbidden implementation path.
- [x] Primeras pruebas deben ser unitarias/mocked. Cualquier cart-probe live requiere dominio green aprobado, limite bajo, UA honesto, cleanup probado y log de no-checkout. Evidence: `tests/test_prober_*.py` use fake transports; no live cart-probe executed; live calibration remains `[blocked]`.
- [x] Cloudflare/CAPTCHA/challenge/red tier => `blocked_by_antibot`, no forzar; silverback/red no se probea. Evidence: `researcher.report.md` identifies red/captcha Woo domains as excluded; `security.report.md` and tests verify 403/429/challenge block/cooldown.
- [x] Sesion efimera por producto/variante; `/cart/clear.js` o equivalente al terminar, incluso en error; no dejar carritos sucios. Evidence: Shopify and Woo cleanup tests in `tests/test_prober_shopify.py` and `tests/test_prober_woo.py`; dirty cleanup returns `ERROR/cart_cleanup_failed`.
- [x] Kill-switch por dominio: primer 403/429/challenge sostenido aborta probe del dominio, cae a visible/unknown, cooldown 24-48h, metrica `competitor_crawl_block_total{domain,reason}`. Evidence: `src/prober/killswitch.py` default cooldown 36h; `tests/test_prober_contract.py`, `tests/test_prober_shopify.py`, `tests/test_prober_woo.py`, `tests/test_prober_service.py`.
- [x] Egress aislado definido antes de produccion; no activar CronJob/nocturno ni replicas productivas en F4. Evidence: `k8s/prober-networkpolicy.yaml` default-deny `egress: []`; `k8s/prober-deployment.yaml` `replicas: 0`; `kubectl apply --dry-run=server -k k8s` PASS; no CronJob.
- [x] No F6: no tocar comparacion viva `prices.py`/`intel.py`, no depender de `PRODUCT_MATCH` nuevo. Evidence: `verifier.report.md` diff scope; no `prices.py`/`intel.py` found/touched; no F6 opened.
- [x] No F7: no scheduling productivo, no CronJob activo. Evidence: `verifier.report.md`; no CronJob/Service added; prober Deployment disabled.
- [x] No secretos/cookies/HTML crudo en logs/RSO; snapshots solo URI/key si aplica. Evidence: `security.report.md`; `src/prober` has no logging/print and no headers/cookie/raw HTML fields.
- [x] Git: rama `codex/competitor-crawler-F4-cart-probe`; fetch+rebase antes de commit; push inmediato; nunca force-push ni tocar `deploy/prod` directamente. Evidence: branch verified by `git status --short --branch`; closeout ran `git fetch origin` and `git rebase origin/codex/competitor-crawler-F4-cart-probe` -> up to date before commit; push immediate pending in same closeout; no force-push.

## Acceptance Criteria
- [x] **Research de plataforma completo.** Confirmar repo destino (`skirmshop-stock-prober` nuevo vs modulo en repo actual), patrones existentes de Playwright/HTTP, dominios green candidatos, robots/antibot/tier, y si existe egress aislado. Evidence: `researcher.report.md`; branch/gates confirmed; recommendation is in-repo `src/prober/` + disabled deployment; all current green targets are `generic_html`; no green Shopify/Woo; no NetworkPolicy exists.
- [x] **Contrato de datos F4 definido.** `probe_stock(...)` o equivalente devuelve `domain`, `product_key`, `variant_id/url`, `stock_qty`, `stock_status`, `stock_method=cart_probe`, `probe_status`, `block_reason`, `cleanup_status`, `observed_at`, y errores explicitos. Evidence: `architect.report.md`; `ProbeResult` contract and mapper to F3 `Observation` defined; no F3 schema change required.
- [x] **Shopify probe mock PASS.** Busqueda binaria o parse de 422 para maxima cantidad disponible; tests cubren in-stock, unavailable, qty limite, 403/429, challenge y cleanup. Evidence: `backend-shopify.report.md`; `tests/test_prober_shopify.py`; `pytest -q` -> `81 passed in 0.20s`.
- [x] **WooCommerce probe mock PASS.** Usa `quantity_limits.maximum` si disponible o respuesta add-to-cart; tests cubren mismas ramas de seguridad. Evidence: `backend-woo.report.md`; `tests/test_prober_woo.py`; `pytest -q` -> `91 passed in 0.31s`.
- [x] **Generic probe policy PASS.** GenericHtml solo probea si hay patron add-to-cart seguro y testeado; si no, `unknown/blocked` sin forzar. Evidence: `backend-generic.report.md`; `GenericProber` default-deny returns `SKIPPED/no_safe_pattern` without transport calls; `pytest -q` -> `70 passed in 0.24s`.
- [x] **Cleanup verificado.** Cada probe termina con clear cart o cleanup equivalente; tests prueban cleanup tambien cuando add-to-cart falla. Evidence: `backend-shopify.report.md`, `backend-woo.report.md`; Shopify cleanup success/fail tests; Woo cleanup success/fail tests; `pytest -q` -> `98 passed in 0.27s`.
- [x] **Kill-switch verificado.** Smoke mocked 403/429/challenge activa cooldown y bloquea probes posteriores del dominio; metrica/log estructurado presente. Evidence: `backend-foundation.report.md`, `backend-shopify.report.md`, `backend-woo.report.md`, `backend-service.report.md`; DomainGuard, Shopify, Woo and service cooldown tests; `pytest -q` -> `98 passed in 0.27s`.
- [x] **Integracion F3 PASS.** Resultado cart-probe se puede escribir via F3 writer como observacion append-only con `stock_qty` numerico y `stock_method=cart_probe`; idempotencia se mantiene. Evidence: `backend-foundation.report.md`; `tests/test_prober_contract.py::test_probed_exact_qty_maps_and_writes_idempotently`; `pytest -q` -> `64 passed in 0.22s`.
- [blocked] **Calibracion muestra 10 PASS.** Tabla probe vs visible/ground-truth para dominio green aprobado; discrepancias clasificadas; cero checkout/login; cleanup log por producto. Evidence: `researcher.report.md`, `security.report.md`, `verifier.report.md`, `codex-audit.report.md`, `f4b-target-discovery.report.md`; blocked because current green targets are all `generic_html`, zero green Shopify domains, and Woo domains are red/captcha. F4b read-only refresh to `/tmp` reproduced the same fingerprint with no drift.
- [x] **Security PASS.** No secretos, no cookies persistentes, no PII, no CAPTCHA solving, no checkout, rate limits/cooldown, dominio red bloqueado. Evidence: `security.report.md`; `verifier.report.md`; Codex RSO security scan of `src/prober`, `tests/test_prober_*`, `k8s`, and F4 reports.
- [x] **DevOps PASS.** Microservicio/manifiestos si existen quedan disabled por defecto (`replicas:0`/activation disabled), egress aislado documentado, server dry-run PASS, no live production schedule. Evidence: `devops.report.md`; `k8s/prober-deployment.yaml` (`replicas: 0`, `automountServiceAccountToken: false`, activation disabled via kustomization label), `k8s/prober-networkpolicy.yaml` (default-deny `egress: []`), `k8s/kustomization.yaml` actualizado; `kubectl kustomize k8s` renders disabled Deployment + NetworkPolicy; `kubectl apply --dry-run=server -k k8s` -> prober Deployment and NetworkPolicy created (server dry run); no CronJob/no live apply.
- [x] **Verifier PASS.** Re-ejecuta tests, smoke kill-switch, diff scope, security scan y evidencia de cleanup/calibracion. Evidence: `verifier.report.md`; `pytest -q` -> 98 passed; `git diff --check` PASS; `python3 -m compileall src tests` PASS; `kubectl apply --dry-run=server -k k8s` PASS; calibration correctly `[blocked]`.

## Specialist Checks
- [x] **rho-researcher** - repo destino, plataformas, dominios candidatos, robots/antibot, egress actual. Evidence: `researcher.report.md`; Claude CLI read-only PASS; live calibration target blocked for lack of green Shopify/Woo.
- [x] **rho-architect** - contrato probe, boundaries F4/F3/F6/F7, modelo de cooldown/metrics. Evidence: `architect.report.md`; Claude CLI read-only PASS; live calibration remains blocked.
- [x] **rho-backend** - probe core, transports mockables, integracion F3 writer, tests. Evidence: `backend-foundation.report.md`, `backend-generic.report.md`, `backend-shopify.report.md`, `backend-woo.report.md`, `backend-service.report.md`; `pytest -q` -> `98 passed in 0.27s`; `git diff --check` PASS; `python3 -m compileall src tests` PASS.
- [x] **rho-devops** - microservicio/k8s/egress/dry-run/no Cron activo. Evidence: `devops.report.md`; manifests disabled creados (`prober-deployment.yaml` replicas:0, `prober-networkpolicy.yaml` default-deny egress, kustomization actualizado); Codex RSO re-ran `kubectl kustomize k8s` and `kubectl apply --dry-run=server -k k8s`; no CronJob, no Service, no live apply.
- [x] **rho-security** - anti-bot/legal/no checkout/no login/no CAPTCHA/no dirty carts. Evidence: `security.report.md`; PASS mock-only, calibration blocked.
- [x] **rho-verifier** - re-ejecuta tests/smokes/diff/evidencia. Evidence: `verifier.report.md`; independent PASS mock-only, calibration blocked.
- [x] **Codex/RSO auditor** - decide PASS/BLOCKED y no abre F6 hasta PASS. Evidence: `codex-audit.report.md`; F4 mock/dry-run PASS, full F4 BLOCKED on calibration, F6 not opened.

## Status (log datado, append-only)
- 2026-06-25T00:34:26+02:00 - OPEN: F4 abierta tras F3 PASS. Rama `codex/competitor-crawler-F4-cart-probe` creada y publicada. Pendiente research Claude CLI antes de implementar probe o tocar dominios reales.
- 2026-06-25T00:52:00+02:00 - RESEARCH PASS / LIVE CALIBRATION BLOCKED: `rho-researcher` read-only confirma que no hay Shopify/Woo green; todos los green son `generic_html`, no hay NetworkPolicy/egress isolation, y F3 writer ya soporta `cart_probe`. RSO decide avanzar con mocks + modulo in-repo y mantener calibracion live bloqueada hasta aprobar target seguro.
- 2026-06-25T00:58:00+02:00 - ARCHITECT PASS: `rho-architect` define `ProbeResult`, mapper a F3 `Observation`, `src/prober/` in-repo, Generic default-deny, `DomainGuard` cooldown/metrics y matriz T1-T20. Live calibration sigue bloqueada por falta de target green Shopify/Woo y NetworkPolicy.
- 2026-06-25T01:08:00+02:00 - BACKEND FOUNDATION PASS: `rho-backend` implementa contrato `ProbeResult`, mapper a F3, `DomainGuard` cooldown y metricas in-memory. Codex RSO elimina scaffolding fuera de scope no cubierto. Evidencia: `pytest -q` 64 passed, `git diff --check` PASS, `python3 -m compileall src tests` PASS. Pendiente Shopify/Woo/Generic probers.
- 2026-06-25T01:18:00+02:00 - BACKEND GENERIC PASS: transporte mockable y `GenericProber` default-deny implementados; cero llamadas de red, metrica skipped y tests PASS. Pendiente Shopify/Woo/service.
- 2026-06-25T01:31:00+02:00 - BACKEND SHOPIFY PASS: `ShopifyProber` mock-only implementado con 422 parse, binary search, uncapped, unavailable, 403/429/challenge kill-switch, cleanup y unexpected-status errors. Evidencia: `pytest -q` 81 passed. Pendiente Woo/service.
- 2026-06-25T01:41:00+02:00 - BACKEND WOO PASS: `WooProber` mock-only implementado con `quantity_limits.maximum`, qty desconocida, out-of-stock, 403/429/challenge kill-switch, cleanup y errores. Evidencia: `pytest -q` 91 passed. Pendiente service facade y DevOps/Security/Verifier.
- 2026-06-25T01:50:00+02:00 - BACKEND MOCK PASS: `probe_stock` service facade implementado. Backend F4 mock-only completo: contract, guard, metrics, Generic default-deny, Shopify, Woo, service y F3 mapper. Evidencia: `pytest -q` 98 passed. Pendiente DevOps/Security/Verifier y calibracion live bloqueada.
- 2026-06-25T02:05:00+02:00 - DEVOPS PASS: `rho-devops` crea manifests disabled `k8s/prober-deployment.yaml` (replicas:0, automountServiceAccountToken:false, activation disabled), `k8s/prober-networkpolicy.yaml` (default-deny egress []) y actualiza `k8s/kustomization.yaml`. Codex RSO re-ejecuta `kubectl kustomize k8s` y `kubectl apply --dry-run=server -k k8s`: prober Deployment y NetworkPolicy created (server dry run). No commit/push por rol implementer.
- 2026-06-25T01:28:00+02:00 - SECURITY/VERIFIER/CODEX AUDIT: `rho-security` PASS mock-only, `rho-verifier` PASS mock-only re-ejecutando `pytest -q` (98 passed), `git diff --check`, `python3 -m compileall src tests` y `kubectl apply --dry-run=server -k k8s`. Codex RSO re-ejecuta los mismos gates. F4 full gate queda `BLOCKED` por calibracion muestra-10 sin dominio green Shopify/Woo aprobado. No se abre F6.
- 2026-06-25T02:29:00+02:00 - F4B TARGET DISCOVERY BLOCKED: `rho-researcher` y Codex RSO verifican `scripts/fingerprint_domains.py` como GET-only, ejecutan refresh read-only a `/tmp`, comparan contra `data/competitors/fingerprint.json` y no hay drift. Top30 actual sigue con 5 green todos `generic_html`, 0 Shopify y 2 Woo red/captcha. No se abre F6.
- 2026-06-25T03:08:00+02:00 - F4C ENDPOINT-SCOPED ANTIBOT PASS / LIVE CART BLOCKED: `rho-backend` corrige `scripts/fingerprint_domains.py` para que el antibot sea endpoint-scoped: Shopify/Woo detectado por endpoint JSON 200 con shape correcta y sin markers usa `effective_antibot` del endpoint (root preservado en `evidence.root_antibot`), generic/unknown/html-inferred siguen usando root antibot, y un endpoint de datos 403/429/captcha/cloudflare/challenge es fail-closed => red. Evidence: `backend-fingerprint.report.md`, `f4c-target-expansion.report.md`, `codex-f4c-audit.report.md`; `tests/test_fingerprint_domains.py`; Codex RSO `pytest -q tests/test_fingerprint_domains.py` -> 20 passed, `pytest -q` -> 118 passed, `python3 -m compileall scripts src tests` PASS, `git diff --check` PASS. `rho-security` and `rho-verifier` PASS. GET-only re-fingerprint to `/tmp` finds top30 endpoint-green Woo candidates (`novritsch.com`, `silverback-airsoft.com`) and expanded endpoint-green candidates (`airsoftmania.eu`, `justbbguns.co.uk`, `socomtactical.net`). **Calibracion live muestra-10 sigue `[blocked]`: endpoint green = lectura GET, no autorizacion de POST cart-probe. No se abre F6.
